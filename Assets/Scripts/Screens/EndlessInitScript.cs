using UnityEngine;
using System.Collections;

public class EndlessInitScript : MonoBehaviour {

	// Use this for initialization
	void Start () {
		LevelSelection.CURRENT_GAMEMODE = GameMode.endless;
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
